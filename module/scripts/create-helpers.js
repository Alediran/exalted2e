export default class CreateHelper {
  static async SetAbilities(actorCopy, type, era) {
    console.log(`Exalted | Set ${type} Abilities - ${era}`);
  }

  static async SetSolarAbilities(actor, era) {}
}
