<div class="countermagic-dialog">
  <div class="spell-info">
    <strong>{{spellName}}</strong>
    <span class="tradition-circle">{{traditionLabel}} · {{circleLabel}}</span>
    {{#if casterName}}<span class="caster-name">{{localize "EX2E.CountermagicCaster"}}: {{casterName}}</span>{{/if}}
  </div>

  {{#if (gt charmRangeList.length 1)}}
  <div class="field-group">
    <label>{{localize "EX2E.CountermagicCharm"}}</label>
    <select name="charmIndex">
      {{#each charmRangeList as |cr i|}}
      <option value="{{i}}" data-min="{{cr.moteMin}}" data-max="{{cr.moteMax}}">{{cr.charm.name}}</option>
      {{/each}}
    </select>
  </div>
  {{else}}
  <input type="hidden" name="charmIndex" value="0">
  <div class="charm-auto">{{localize "EX2E.CountermagicUsing"}}: <strong>{{charmRangeList.[0].charm.name}}</strong></div>
  {{/if}}

  {{!-- Single authoritative motes value; updated by slider input or charm-select change --}}
  <input type="hidden" name="motes" class="motes-value" value="{{moteMin}}">

  <div class="field-group mote-range-group"{{#unless hasRange}} style="display:none"{{/unless}}>
    <label>{{localize "EX2E.CountermagicMotes"}} ({{moteMin}}–{{moteMax}})</label>
    <input type="range" class="mote-slider" min="{{moteMin}}" max="{{moteMax}}" value="{{moteMin}}">
    <span class="mote-display">{{moteMin}}m</span>
  </div>
  <div class="field-group mote-fixed-group"{{#if hasRange}} style="display:none"{{/if}}>
    <label>{{localize "EX2E.CountermagicMotes"}}: <strong class="mote-fixed-value">{{moteMin}}m</strong></label>
  </div>

  <div class="pool-info">
    <span>{{localize "EX2E.MotesPeripheralShort"}}: {{peripheralVal}}/{{peripheralMax}}</span>
    <span>{{localize "EX2E.MotesPersonalShort"}}: {{personalVal}}/{{personalMax}}</span>
  </div>

  <div class="dialog-buttons">
    <button type="button" data-action="confirm" class="btn-confirm">
      <i class="fa-solid fa-bolt"></i> {{localize "EX2E.CountermagicConfirm"}}
    </button>
    <button type="button" data-action="cancel" class="btn-cancel">
      {{localize "EX2E.Cancel"}}
    </button>
  </div>

</div>
