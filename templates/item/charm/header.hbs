<div class="item-header charm-header">
  <img class="item-portrait" src="{{item.img}}" alt="{{item.name}}"
       data-action="editImage" {{#unless isEditable}}disabled{{/unless}}>
  <div class="item-header-fields">
    <input class="item-name-input" type="text" name="name" value="{{item.name}}"
           {{#unless isEditable}}readonly{{/unless}}>
    <div class="charm-identity-row">
      <!-- Exalt Type -->
      {{#unless system.isSubmodule}}
      <div class="field-group">
        <label>{{localize "EX2E.SplatType"}}</label>
        <select name="system.exaltType" {{#unless isEditable}}disabled{{/unless}}>
          {{#each splatTypes}}
          <option value="{{this.value}}" {{#if (eq this.value ../system.exaltType)}}selected{{/if}}>{{this.label}}</option>
          {{/each}}
        </select>
      </div>
      {{/unless}}
      <!-- Ability / Attribute (hidden for Infernal; Yozi shown instead) -->
      {{#unless (eq system.exaltType "infernal")}}
      <div class="field-group">
        <label>{{abilityFieldLabel}}</label>
        <select name="system.ability" {{#unless isEditable}}disabled{{/unless}}>
          {{#each abilities}}
          <option value="{{this.value}}" {{#if (eq this.value ../system.ability)}}selected{{/if}}>{{this.label}}</option>
          {{/each}}
        </select>
      </div>
      {{/unless}}
      {{#if (eq system.exaltType "infernal")}}
      <div class="field-group">
        <label>{{localize "EX2E.CharmYoziPatron"}}</label>
        <select name="system.yoziPatron" {{#unless isEditable}}disabled{{/unless}}>
          <option value="" {{#unless system.yoziPatron}}selected{{/unless}}>—</option>
          {{#each yoziPatronOptions}}
          <option value="{{this.key}}" {{#if (eq this.key ../system.yoziPatron)}}selected{{/if}}>{{this.label}}</option>
          {{/each}}
        </select>
      </div>
      {{/if}}
      {{#if (eq system.exaltType "sidereal")}}
      <div class="field-group">
        <label>{{localize "EX2E.MaidenAffiliation"}}</label>
        <select name="system.maidenAffiliation" {{#unless isEditable}}disabled{{/unless}}>
          {{#each maidenAffiliationOptions}}
          <option value="{{this.value}}" {{#if (eq this.value ../system.maidenAffiliation)}}selected{{/if}}>{{this.label}}</option>
          {{/each}}
        </select>
      </div>
      {{/if}}
      {{#if (eq system.exaltType "martialarts")}}
      <div class="field-group">
        <label>{{localize "EX2E.MartialArtsStyleName"}}</label>
        <input type="text" name="system.martialArtsStyleName" value="{{system.martialArtsStyleName}}"
               placeholder="e.g. snake-style" {{#unless isEditable}}readonly{{/unless}}>
      </div>
      <div class="field-group">
        <label>{{localize "EX2E.MartialArtsElement"}}</label>
        <select name="system.martialArtsElement" {{#unless isEditable}}disabled{{/unless}}>
          {{#each martialArtsElementOptions}}
          <option value="{{this.value}}" {{#if (eq this.value ../system.martialArtsElement)}}selected{{/if}}>{{this.label}}</option>
          {{/each}}
        </select>
      </div>
      {{/if}}
    </div>
    <!-- Cost Summary -->
    <div class="charm-cost-summary">
      <strong>{{localize "EX2E.Cost"}}:</strong>
      {{moteCostString system.cost}}
      {{#unless system.isSubmodule}}&nbsp;·&nbsp; <strong>Type:</strong> {{system.charmType}}{{/unless}}
      &nbsp;·&nbsp; <strong>Duration:</strong> {{system.duration}}
    </div>
  </div>
</div>
